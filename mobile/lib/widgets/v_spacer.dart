import 'package:flutter/material.dart';

class VSpacer extends StatelessWidget {
  const VSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
